
如果我要用一个仿真软件，来做机器人强化学习的项目

一个网络相关的代码

一个环境相关的代码

一个训练流程

也就是说在训练流程中不断调用环境的接口得到各种信息，并把信息交给网络，再让网络反馈在流程中交给环境就行了

所以项目也应该以这三大块为主

日志数据之类可以算第四块，模型应该算在数据里面或者网络里面

# 关于环境相关代码

## 基础交互
首先很明显的
`Webots_interfaces.py`是机器人的控制文件

先从webots的controller里面调用Robot
然后自己写了一个class Darwin类，来管理Robot里面的接口，统一得到自己想要的参数，以及去映射动作

**原则上，任何动作，传感器或者其他的行为逻辑都要先看Webots_interfaces.py里面是否有所实现**


# 关于网络相关

使用了hppo和hppo_01

# 关于训练流程使用

python_scripts\PPO\PPO_episoid_1.py

该流程文件应该保证绝对的简洁与易读性

关于模型的保存，训练的管理等与训练相关的文件都放置在E:\project_MultiAgent_h_change\python_scripts\PPO\preparation_tool