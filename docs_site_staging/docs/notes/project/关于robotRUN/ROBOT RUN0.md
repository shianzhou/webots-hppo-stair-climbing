决策层的动作执行

决策层是通过网络的输出01来判断进行哪一个阶段，所以也就是输出01值了，事实上由于01值直接就由网络给出，所以当前文件实际上做不了什么

但是决策层奖励值的计算应该在这里给出，然后判断逻辑应该在这里给出


decision = 0 抓取 ，抓取失败 =》重新决策
decision = 0 抓取，抓取成功，保持动作 =》重新决策

decision = 1 抬腿，当前未抓取，重新决策
decision = 1 抬腿，当前抓取成功，执行抬腿，